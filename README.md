# GTASCII

An gta-like game but in 2d, top-down that runs in the terminal with ascii characters rendering

This game currently only compiles and runs via the Makefile for MacOS but you can edit the library paths to be able to compile for other operating systems

The only library in use is ncurses which provides the tools for rendering into the terminal
